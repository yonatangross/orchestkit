# OrchestKit Configuration
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed
#
# RECOMMENDED: Use 1Password instead of .env for secrets.
# The .mcp.json config uses `op read` to fetch keys from 1Password at startup.
# See src/skills/configure/references/mcp-config.md for setup instructions.

# Tavily Configuration (Optional)
# Enhanced web search, content extraction, and site crawling
# Get it from: https://app.tavily.com (free: 1000 credits/month)
# Store in 1Password: op item create --category "API Credential" --title "Tavily API Key" "API Key=tvly-..."
# TAVILY_API_KEY=tvly-your-api-key-here  # Only if not using 1Password
