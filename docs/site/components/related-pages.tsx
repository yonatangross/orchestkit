// Generated by OrchestKit Claude Plugin
// Created: 2026-09-19
//
// Presentational "Related" block for reference pages. The list itself is
// computed at build time by lib/related-pages.ts (deterministic scorer,
// optional Jev re-rank); this component only renders it. Server component:
// no client JS is added for a list of links.

import Link from "next/link";
import type { RelatedItem } from "@/lib/related-pages";

export function RelatedPages({ items }: { items: readonly RelatedItem[] }) {
	if (items.length === 0) return null;
	return (
		<nav
			aria-label="Related pages"
			className="mt-8 rounded-xl border border-fd-border bg-fd-card p-4"
		>
			<h2 className="mb-3 text-sm font-semibold text-fd-foreground">Related</h2>
			<ul className="flex flex-col gap-2">
				{items.map((item) => (
					<li key={item.url}>
						<Link
							href={item.url}
							className="text-sm text-fd-muted-foreground underline-offset-4 transition-colors hover:text-fd-foreground hover:underline"
						>
							{item.title}
						</Link>
					</li>
				))}
			</ul>
		</nav>
	);
}
