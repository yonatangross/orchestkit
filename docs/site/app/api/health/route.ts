// Generated by OrchestKit Claude Plugin
// Created: 2026-06-05

import { SITE } from "@/lib/constants";

// Liveness endpoint referenced as the `status` relation of the API catalog
// (/.well-known/api-catalog). Intentionally dependency-free so it reflects only
// whether the deployment is serving.
export const dynamic = "force-dynamic";

export function GET() {
	return Response.json(
		{ status: "ok", service: SITE.name, version: SITE.version },
		{ headers: { "Cache-Control": "no-store" } },
	);
}
