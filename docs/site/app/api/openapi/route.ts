// Generated by OrchestKit Claude Plugin
// Created: 2026-06-05

import { buildOpenApiSpec } from "@/lib/openapi/spec";

// OpenAPI 3.1 description of the public, read-only OrchestKit API. Referenced as
// the `service-desc` relation of the API catalog (/.well-known/api-catalog) so
// agents can discover the API surface and call it via function-calling. No auth.
// The spec object lives in lib/openapi/spec.ts so this JSON surface and the YAML
// twin (app/api/openapi-yaml) stay in lockstep. Also served at /openapi.json and
// /api/openapi.json via rewrites (next.config.mjs) — the paths agents probe.
export const revalidate = false;

export function GET() {
	return new Response(JSON.stringify(buildOpenApiSpec(), null, 2), {
		headers: {
			"Content-Type": "application/json; charset=utf-8",
			"Cache-Control": "public, max-age=3600",
		},
	});
}
