// Generated by OrchestKit Claude Plugin
// Created: 2026-06-12

// Human-facing (home) route-group pages, surfaced in the sitemap. Kept in a
// zero-import module so __tests__/sitemap.test.ts can assert filesystem sync
// without pulling in the fumadocs source runtime. Synced to app/(home)/* by that
// test — the guard that stops a new marketing page shipping invisible to
// crawlers (the bug behind orank's "developer resources invisible" flag: the
// pages from #2403 served 200 but were absent from the sitemap).
export const STATIC_PAGES = [
  "/about",
  "/alternatives",
  "/best-claude-code-plugins",
  "/claude-agent-sdk-vs-claude-code-plugins",
  "/compare",
  "/contact",
  "/developers",
  "/pricing",
  "/privacy",
  "/status",
  "/terms",
] as const;
