name: OrchestKit Eval Pipeline

on:
  workflow_dispatch:
    inputs:
      scope:
        description: 'Eval scope: full, skills-only, hooks-only, changed'
        required: false
        default: 'full'
  schedule:
    - cron: '0 0 * * 0' # Sunday midnight UTC

permissions:
  contents: read
  actions: read

# Fork each subagent invocation per #1545 (CC 2.1.121+).
# Eliminates cross-eval state leak (memory cache, .claude/chain, ToolSearch)
# that previously produced flaky scores and ~5-10% retry rate.
# Older CC silently ignores the env var.
env:
  CLAUDE_CODE_FORK_SUBAGENT: 1

jobs:
  eval:
    name: Run OrchestKit Evaluation
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci
          cd src/hooks && npm ci

      - name: Build hooks
        run: cd src/hooks && npm run build

      - name: Build plugins
        run: npm run build

      - name: Run unit tests
        id: unit-tests
        run: npm test 2>&1 | tee test-output.txt
        continue-on-error: true

      - name: Run security tests
        id: security-tests
        run: npm run test:security 2>&1 | tee security-output.txt
        continue-on-error: true

      - name: Run TypeScript type check
        id: typecheck
        run: npm run typecheck 2>&1 | tee typecheck-output.txt
        continue-on-error: true

      - name: Run skill validation
        id: skill-validation
        run: npm run test:skills 2>&1 | tee skills-output.txt
        continue-on-error: true

      - name: Run agent validation
        id: agent-validation
        run: npm run test:agents 2>&1 | tee agents-output.txt
        continue-on-error: true

      - name: Run manifest checks
        id: manifest-checks
        run: npm run test:manifests 2>&1 | tee manifests-output.txt
        continue-on-error: true

      - name: Install Claude Code CLI
        if: inputs.scope != 'hooks-only'
        # Scorecard pinned-deps (npmCommand): accepted risk — npm has no SHA-pin; --ignore-scripts
        # mitigates the postinstall. Dropped from code-scanning by the SARIF filter in scorecard.yml.
        # `--ignore-scripts` skips the package's own postinstall which
        # fetches the native `claude` binary; we re-run it explicitly so
        # scripts/run-eval.sh (which calls `claude -p --bare`) doesn't
        # fast-fail with "claude native binary not installed."
        run: |
          set -euo pipefail
          npm install -g @anthropic-ai/claude-code@2.1.148 --ignore-scripts
          node "$(npm root -g)/@anthropic-ai/claude-code/install.cjs"
          claude --version
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Run skill trigger detection eval
        if: inputs.scope == 'full' || inputs.scope == 'skills-only' || inputs.scope == 'changed'
        id: skill-eval
        run: |
          chmod +x scripts/run-eval.sh
          scripts/run-eval.sh --scope "$EVAL_SCOPE" 2>&1 | tee eval-output.txt
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          EVAL_SCOPE: ${{ inputs.scope || 'full' }}
        continue-on-error: true

      - name: Compile eval results
        env:
          EVAL_SCOPE: ${{ inputs.scope || 'full' }}
          GIT_SHA: ${{ github.sha }}
          GIT_REF: ${{ github.ref }}
          UNIT_TESTS_OUTCOME: ${{ steps.unit-tests.outcome }}
          SECURITY_TESTS_OUTCOME: ${{ steps.security-tests.outcome }}
          TYPECHECK_OUTCOME: ${{ steps.typecheck.outcome }}
          SKILL_VALIDATION_OUTCOME: ${{ steps.skill-validation.outcome }}
          AGENT_VALIDATION_OUTCOME: ${{ steps.agent-validation.outcome }}
          MANIFEST_CHECKS_OUTCOME: ${{ steps.manifest-checks.outcome }}
          SKILL_EVAL_OUTCOME: ${{ steps.skill-eval.outcome }}
        run: |
          node -e "
          const fs = require('fs');
          const read = f => { try { return fs.readFileSync(f, 'utf8'); } catch { return 'N/A'; } };
          const results = {
            timestamp: new Date().toISOString(),
            scope: process.env.EVAL_SCOPE,
            git_sha: process.env.GIT_SHA,
            git_ref: process.env.GIT_REF,
            suites: {
              unit_tests: { status: process.env.UNIT_TESTS_OUTCOME, output: read('test-output.txt').slice(-2000) },
              security_tests: { status: process.env.SECURITY_TESTS_OUTCOME, output: read('security-output.txt').slice(-2000) },
              typecheck: { status: process.env.TYPECHECK_OUTCOME, output: read('typecheck-output.txt').slice(-2000) },
              skill_validation: { status: process.env.SKILL_VALIDATION_OUTCOME, output: read('skills-output.txt').slice(-2000) },
              agent_validation: { status: process.env.AGENT_VALIDATION_OUTCOME, output: read('agents-output.txt').slice(-2000) },
              manifest_checks: { status: process.env.MANIFEST_CHECKS_OUTCOME, output: read('manifests-output.txt').slice(-2000) },
              skill_eval: { status: process.env.SKILL_EVAL_OUTCOME, output: read('eval-output.txt').slice(-2000) }
            },
            pass_count: [process.env.UNIT_TESTS_OUTCOME, process.env.SECURITY_TESTS_OUTCOME, process.env.TYPECHECK_OUTCOME, process.env.SKILL_VALIDATION_OUTCOME, process.env.AGENT_VALIDATION_OUTCOME, process.env.MANIFEST_CHECKS_OUTCOME].filter(s => s === 'success').length,
            total_count: 6
          };
          fs.writeFileSync('eval-results.json', JSON.stringify(results, null, 2));
          console.log(JSON.stringify(results, null, 2));
          "

      - name: Upload eval results
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: eval-results
          path: |
            eval-results.json
            test-output.txt
            security-output.txt
            typecheck-output.txt
            skills-output.txt
            agents-output.txt
            manifests-output.txt
            eval-output.txt
          retention-days: 90

      - name: Fail if critical suites failed
        env:
          SECURITY_TESTS_OUTCOME: ${{ steps.security-tests.outcome }}
          UNIT_TESTS_OUTCOME: ${{ steps.unit-tests.outcome }}
        run: |
          if [ "$SECURITY_TESTS_OUTCOME" = "failure" ]; then
            echo "::error::Security tests FAILED — blocking"
            exit 1
          fi
          if [ "$UNIT_TESTS_OUTCOME" = "failure" ]; then
            echo "::error::Unit tests FAILED"
            exit 1
          fi
