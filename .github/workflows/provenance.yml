name: provenance

on:
  pull_request:
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: read

jobs:
  lint:
    uses: Yonatan-HQ/hq-ext-plugin/.github/workflows/provenance-lint.yml@dev
    with:
      required: false # advisory week; flip to true 2026-09-22
