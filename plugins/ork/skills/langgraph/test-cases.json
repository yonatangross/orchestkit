{
  "skill": "langgraph",
  "version": "2.0.0",
  "testCases": [
    {
      "id": "state-accumulating",
      "rule": "state-typeddict",
      "query": "Design state that accumulates findings from multiple agents",
      "expectedBehavior": [
        "Uses TypedDict for defining the state schema with typed fields",
        "Uses Annotated[list, add] for accumulating fields",
        "Imports operator.add as the reducer function for list accumulation",
        "Nodes return partial updates, not full state"
      ]
    },
    {
      "id": "state-context-schema",
      "rule": "state-typeddict",
      "query": "Add configuration that persists across all nodes like temperature",
      "expectedBehavior": [
        "Creates a context_schema dataclass to hold cross-node configuration",
        "Passes the context_schema to the StateGraph constructor at initialization",
        "Accesses shared configuration via get_context() inside individual node functions",
        "Keeps config separate from workflow state"
      ]
    },
    {
      "id": "state-pydantic",
      "rule": "state-pydantic",
      "query": "Use Pydantic for validated state with defaults",
      "expectedBehavior": [
        "Uses Pydantic BaseModel instead of TypedDict for validated state definitions",
        "Adds Field() with defaults and descriptions",
        "Enables automatic validation on state updates to catch invalid data early",
        "Handles validation errors gracefully with informative error messages to the user"
      ]
    },
    {
      "id": "routing-conditional",
      "rule": "routing-conditional",
      "query": "Add conditional routing based on whether content needs review",
      "expectedBehavior": [
        "Uses add_conditional_edges with a routing function that inspects current state",
        "Returns a string matching one of the defined edge names from the routing function",
        "Includes all possible routes in edge map",
        "Has a fallback route or END condition to prevent unhandled edge cases"
      ]
    },
    {
      "id": "routing-retry",
      "rule": "routing-retry-loops",
      "query": "Implement a retry loop that attempts up to 3 times then fails",
      "expectedBehavior": [
        "Tracks retry_count as an integer field in the workflow state schema",
        "Routes back to retry node when count < 3",
        "Routes to failure node when count >= 3",
        "Increments the retry counter on each unsuccessful retry attempt before re-routing"
      ]
    },
    {
      "id": "routing-semantic",
      "rule": "routing-semantic",
      "query": "Create a semantic router that classifies user intent and routes accordingly",
      "expectedBehavior": [
        "Uses LLM with structured output for classification",
        "Returns valid route names that match the defined edge map entries",
        "Handles unknown or other category inputs with a sensible default fallback route",
        "Uses Literal type for route validation"
      ]
    },
    {
      "id": "parallel-fanout",
      "rule": "parallel-fanout-fanin",
      "query": "Process a list of items in parallel with separate worker nodes",
      "expectedBehavior": [
        "Uses Send() to dispatch to workers",
        "Returns list of Send objects from router",
        "Each Send object includes the relevant state subset for its target worker",
        "Workers run in parallel across all dispatched Send objects concurrently"
      ]
    },
    {
      "id": "parallel-fanin",
      "rule": "parallel-fanout-fanin",
      "query": "Collect results from parallel workers into a single list",
      "expectedBehavior": [
        "Uses Annotated[list, add] reducer to merge results from parallel workers",
        "Workers return partial state updates that get merged by the reducer",
        "Results accumulate automatically via the list reducer without manual aggregation",
        "A single downstream node receives all accumulated results after fan-in completes"
      ]
    },
    {
      "id": "parallel-mapreduce",
      "rule": "parallel-map-reduce",
      "query": "Implement map-reduce: parallel processing then reduce to single output",
      "expectedBehavior": [
        "Map phase uses Send() for parallelization",
        "Reduce node has edges from all workers",
        "Reduce node aggregates accumulated results from all parallel worker outputs",
        "A single final output is produced after the reduce phase completes"
      ]
    },
    {
      "id": "supervisor-basic",
      "rule": "supervisor-basic",
      "query": "Create a supervisor that coordinates two workers: analyzer and validator",
      "expectedBehavior": [
        "Uses StateGraph with a dedicated supervisor node that orchestrates worker routing",
        "Creates worker nodes for analyzer and validator",
        "Workers have edges back to supervisor",
        "Uses add_edge(START, 'supervisor') instead of deprecated set_entry_point() for graph entry",
        "Includes an END condition in routing logic to terminate the workflow gracefully"
      ]
    },
    {
      "id": "supervisor-command",
      "rule": "supervisor-basic",
      "query": "Build a supervisor that updates state and routes in the same step",
      "expectedBehavior": [
        "Imports the Command class from langgraph.types for combined state update and routing",
        "Supervisor returns Command with update and goto",
        "Uses Literal type annotation for type safety",
        "No conditional_edges needed when using Command"
      ]
    },
    {
      "id": "supervisor-roundrobin",
      "rule": "supervisor-round-robin",
      "query": "Implement a supervisor that visits all agents exactly once before finishing",
      "expectedBehavior": [
        "Tracks the set of completed agents in the workflow state to avoid revisits",
        "Checks available agents versus completed agents to determine the next routing target",
        "Routes to quality_gate or END when all done",
        "Prevents infinite loops by enforcing a maximum visit count per agent node"
      ]
    },
    {
      "id": "tools-binding",
      "rule": "tools-bind",
      "query": "Bind tools to an LLM and create a ToolNode for execution",
      "expectedBehavior": [
        "Uses @tool decorator for tool functions",
        "Calls llm.bind_tools(tools) to bind the tool definitions to the LLM instance",
        "Creates ToolNode(tools) for automatic execution of LLM-requested tool calls",
        "Adds agent node and tool node to graph"
      ]
    },
    {
      "id": "tools-approval",
      "rule": "tools-interrupts",
      "query": "Add human approval before executing a delete tool",
      "expectedBehavior": [
        "Uses interrupt() inside the tool function to pause execution and await approval",
        "Shows action details in interrupt payload",
        "Checks the human approval response before executing the destructive operation",
        "Returns a cancelled message if the human rejects the proposed tool action"
      ]
    },
    {
      "id": "tools-dynamic",
      "rule": "tools-dynamic",
      "query": "Select different tools based on user permissions",
      "expectedBehavior": [
        "Reads user permissions from the current state to determine allowed tool access",
        "Filters available tools based on permissions",
        "Binds only allowed tools to LLM",
        "Handles case where no tools available"
      ]
    },
    {
      "id": "checkpoints-postgres",
      "rule": "checkpoints-setup",
      "query": "Add PostgreSQL checkpointing to persist workflow state",
      "expectedBehavior": [
        "Uses PostgresSaver.from_conn_string() to create a persistent checkpoint backend with PostgreSQL",
        "Passes the configured checkpointer to the graph compile() method for persistence",
        "Sets thread_id in config for resumption",
        "Uses async connection for async workflows"
      ]
    },
    {
      "id": "checkpoints-resume",
      "rule": "checkpoints-recovery",
      "query": "Resume a workflow that was interrupted",
      "expectedBehavior": [
        "Uses same thread_id as original run",
        "Calls invoke/ainvoke with None as input",
        "Workflow continues execution from the last saved checkpoint state automatically",
        "Handles case where no checkpoint exists"
      ]
    },
    {
      "id": "checkpoints-debug",
      "rule": "checkpoints-recovery",
      "query": "List all checkpoints for debugging a failed workflow",
      "expectedBehavior": [
        "Uses get_state_history() method to retrieve the full sequence of checkpoint snapshots",
        "Iterates through StateSnapshot objects to inspect state values at each checkpoint",
        "Accesses values, next, and config from snapshots",
        "Can identify which node failed by examining the next field in each snapshot"
      ]
    },
    {
      "id": "human-approval",
      "rule": "human-in-loop-interrupt",
      "query": "Add human approval before executing a dangerous operation",
      "expectedBehavior": [
        "Uses the interrupt() function from langgraph.types to pause and await human input",
        "Passes approval request data to interrupt()",
        "Resumes workflow execution with Command(resume=response) containing the human decision",
        "Handles both approval and rejection cases with distinct control flow branches"
      ]
    },
    {
      "id": "human-edit",
      "rule": "human-in-loop-approval",
      "query": "Let human edit AI-generated content before proceeding",
      "expectedBehavior": [
        "Shows generated content in interrupt payload",
        "Human can modify the generated content or approve it as-is before proceeding",
        "Resume uses the human-edited version of the content for downstream processing",
        "Original preserved if human approves as-is"
      ]
    },
    {
      "id": "human-feedback-loop",
      "rule": "human-in-loop-feedback",
      "query": "Implement review loop until human approves quality",
      "expectedBehavior": [
        "Tracks review_count in the workflow state to limit review iteration cycles",
        "Calls interrupt() on each review cycle to collect human feedback before continuing",
        "Routes back to generation if rejected",
        "Proceeds to next step when approved"
      ]
    },
    {
      "id": "streaming-progress",
      "rule": "streaming-custom-events",
      "query": "Stream progress updates showing percentage complete",
      "expectedBehavior": [
        "Uses get_stream_writer() in each node function to emit custom progress events",
        "Writes custom event with progress data",
        "Client receives custom events via stream_mode='custom' in the stream call",
        "Events include a structured type field and payload with progress data"
      ]
    },
    {
      "id": "streaming-tokens",
      "rule": "streaming-tokens",
      "query": "Stream LLM tokens to the client in real-time",
      "expectedBehavior": [
        "Uses stream_mode='messages' to receive individual LLM tokens as they are generated",
        "LLM node uses the streaming API to emit tokens incrementally to the client",
        "Tokens appear in the output stream as they are generated by the model",
        "Final complete message is assembled from all received token chunks on the client"
      ]
    },
    {
      "id": "streaming-multi",
      "rule": "streaming-modes",
      "query": "Get both state updates and custom events in single stream",
      "expectedBehavior": [
        "Passes a list to stream_mode=['updates', 'custom'] to receive multiple event types simultaneously",
        "Filters events by mode in client",
        "Updates show state changes after each node completes its execution step",
        "Custom mode shows user-defined progress events emitted via stream writer"
      ]
    },
    {
      "id": "subgraphs-invoke",
      "rule": "subgraphs-invoke",
      "query": "Call a subgraph with different state schema from parent",
      "expectedBehavior": [
        "Compiles the subgraph separately as an independent StateGraph before embedding it",
        "Parent node transforms state before invoke",
        "Subgraph uses its own state schema",
        "Parent node transforms the subgraph output back into the parent state schema format"
      ]
    },
    {
      "id": "subgraphs-add-node",
      "rule": "subgraphs-add-as-node",
      "query": "Embed a subgraph that shares state with parent",
      "expectedBehavior": [
        "Subgraph uses same state schema as parent",
        "The add_node method accepts a compiled subgraph directly as a node value",
        "No state transformation is needed because parent and subgraph share the same schema",
        "Subgraph reads and writes parent state fields directly without transformation logic"
      ]
    },
    {
      "id": "subgraphs-checkpoint",
      "rule": "subgraphs-state-mapping",
      "query": "Set up checkpointing that works across subgraphs",
      "expectedBehavior": [
        "Only the parent graph has a checkpointer configured for persisting workflow state",
        "Uses checkpointer=False for subgraphs to delegate checkpointing to the parent graph",
        "Subgraph state included in parent checkpoints",
        "Resume restores subgraph state correctly from the parent graph checkpoint data"
      ]
    },
    {
      "id": "functional-basic",
      "rule": "functional-entrypoint",
      "query": "Create a simple workflow using the Functional API",
      "expectedBehavior": [
        "Imports entrypoint and task from langgraph.func",
        "Decorates individual worker functions with @task to define parallelizable units of work",
        "Decorates the main orchestration function with @entrypoint to define the workflow entry",
        "Calls task().result() to block and retrieve the computed return values from tasks"
      ]
    },
    {
      "id": "functional-parallel",
      "rule": "functional-task",
      "query": "Run multiple tasks in parallel using Functional API",
      "expectedBehavior": [
        "Calls multiple @task functions without .result()",
        "Tasks return futures that run in parallel",
        "Calls .result() on all futures to wait",
        "Results are collected after parallel execution completes across all dispatched tasks"
      ]
    },
    {
      "id": "functional-injectable",
      "rule": "functional-task",
      "query": "Access previous state and store in a @task function",
      "expectedBehavior": [
        "Uses InjectedState type annotation to access the current checkpoint state in tasks",
        "Accesses previous checkpoint data to read persisted state from earlier executions",
        "Uses InjectedStore for cross-thread persistence of data across workflow invocations",
        "Parameters annotated with Injected types are injected automatically by the LangGraph runtime"
      ]
    },
    {
      "id": "checkpoints-store-cross-thread-happy-path",
      "rule": "checkpoints-store",
      "query": "I need user preferences to persist across different conversation threads. How do I store cross-thread data in LangGraph so it's not lost when starting a new thread?",
      "expectedBehavior": [
        "Uses PostgresStore for cross-thread long-term memory alongside the checkpointer",
        "Compiles the graph with both checkpointer and store passed to compile()",
        "Accesses store data in nodes via the store parameter with namespaced keys",
        "Uses aget and aput methods with namespace tuples to read and write cross-thread data"
      ]
    },
    {
      "id": "functional-migration-linear-happy-path",
      "rule": "functional-migration",
      "query": "I have a simple three-step StateGraph pipeline with linear edges. Should I migrate it to the Functional API for cleaner code?",
      "expectedBehavior": [
        "Claude recommends migrating simple linear workflows to Functional API with @entrypoint and @task",
        "Claude converts each node to a @task function and chains them with .result() calls",
        "Claude warns against migrating complex topologies with conditional edges or subgraphs",
        "Claude shows that both APIs support checkpointing, streaming, and human-in-the-loop equally"
      ]
    },
    {
      "id": "parallel-error-isolation-happy-path",
      "rule": "parallel-error-isolation",
      "query": "I'm running three analysis agents in parallel but when one fails the entire workflow crashes and I lose the results from agents that succeeded. How do I isolate failures?",
      "expectedBehavior": [
        "Claude uses asyncio.gather with return_exceptions=True to isolate branch failures",
        "Claude adds per-branch timeout using asyncio.wait_for to prevent slow branches from blocking",
        "Claude separates successes from failures so partial results remain available",
        "Claude logs failures for debugging but continues processing with available successful results"
      ]
    },
    {
      "id": "state-messages-conversation-happy-path",
      "rule": "state-messages",
      "query": "My chatbot agent loses previous messages every time a new node runs because the messages list gets overwritten. How do I preserve conversation history across nodes?",
      "expectedBehavior": [
        "Claude uses MessagesState or Annotated[list, add_messages] reducer for the messages field",
        "Claude explains that add_messages appends new messages instead of overwriting the history",
        "Claude warns that MessageGraph is deprecated in LangGraph v1.0.0 and should not be used",
        "Claude shows nodes returning partial updates with new messages that get appended automatically"
      ]
    },
    {
      "id": "state-reducers-custom-merge-happy-path",
      "rule": "state-reducers",
      "query": "Multiple nodes in my workflow update a shared config dictionary but later nodes overwrite keys set by earlier nodes. How do I merge dictionary updates instead of replacing them?",
      "expectedBehavior": [
        "Claude defines a custom merge_dicts reducer function that deep-merges dictionary updates",
        "Claude uses Annotated[dict, merge_dicts] type annotation for the config field",
        "Claude explains that nodes must return new state objects and never mutate state in place",
        "Claude shows how to use Annotated[str, last_value] to make simple overwrites explicit"
      ]
    },
    {
      "id": "supervisor-priority-routing-happy-path",
      "rule": "supervisor-priority",
      "query": "I have a supervisor coordinating security, implementation, and tutorial agents but security should always run before implementation. How do I enforce priority-based execution order?",
      "expectedBehavior": [
        "Claude implements a priority dictionary mapping each agent to a numeric priority value",
        "Claude selects the next agent using min() on available agents by their priority rank",
        "Claude tracks agents_completed in state to prevent revisiting finished agents",
        "Claude limits the supervisor to three to eight specialists maximum to avoid coordination overhead"
      ]
    },
    {
      "id": "tools-toolnode-automatic-happy-path",
      "rule": "tools-toolnode",
      "query": "I'm manually dispatching tool calls from LLM messages in a loop. Is there a built-in way to handle tool execution with automatic parallel support in LangGraph?",
      "expectedBehavior": [
        "Claude uses the prebuilt ToolNode from langgraph.prebuilt for automatic tool execution",
        "Claude shows that ToolNode executes multiple tool calls in parallel automatically",
        "Claude adds a tools to agent edge for the ReAct loop pattern",
        "Claude routes based on tool_calls presence in the last message to decide continuation"
      ]
    },
    {
      "id": "platform-deployment-config-happy-path",
      "rule": "platform-deployment",
      "query": "I want to deploy my LangGraph agent as an API with persistence and streaming. How do I configure langgraph.json and use the CLI for local development and Docker deployment?",
      "expectedBehavior": [
        "Claude creates a langgraph.json with dependencies, graphs, and env configuration",
        "Claude uses langgraph dev for local development with hot reload and in-memory state",
        "Claude uses langgraph build for Docker image creation for self-hosted deployments",
        "Claude shows the Assistants API for creating multiple configurations from a single deployed graph"
      ]
    },
    {
      "id": "platform-remote-graph-invoke-happy-path",
      "rule": "platform-remote-graph",
      "query": "I have a LangGraph agent deployed on Platform and need to call it from my Python application. How do I invoke it programmatically with streaming support?",
      "expectedBehavior": [
        "Claude imports RemoteGraph from langgraph.pregel.remote for the client interface",
        "Claude shows invoke and ainvoke methods with thread_id in config for stateful conversations",
        "Claude demonstrates astream with stream_mode for real-time event streaming from the remote graph",
        "Claude shows adding RemoteGraph as a node in a parent graph for distributed orchestration"
      ]
    },
    {
      "id": "routing-cross-graph-escalation-happy-path",
      "rule": "routing-cross-graph",
      "query": "My child subgraph needs to escalate complex tasks back to the parent graph's supervisor node. How do I route from a subgraph node to a node in the parent graph?",
      "expectedBehavior": [
        "Claude uses Command with graph=Command.PARENT to navigate from child to parent graph",
        "Claude includes state updates in the Command that map to the parent state schema",
        "Claude ensures shared state keys updated via Command.PARENT have reducers in parent state",
        "Claude warns not to wrap Command returns in try/except because it uses exceptions internally"
      ]
    },
    {
      "id": "platform-double-texting-strategy-happy-path",
      "rule": "platform-double-texting",
      "query": "Users in my chatbot sometimes send a correction message while the previous response is still generating, causing corrupted state. How do I handle concurrent messages on the same thread?",
      "expectedBehavior": [
        "Claude sets multitask_strategy on every runs.create call to prevent race conditions",
        "Claude explains all four strategies: reject, rollback, enqueue, and interrupt with use cases",
        "Claude recommends rollback for chatbots where user corrections should override in-progress work",
        "Claude warns that without a multitask_strategy concurrent messages on the same thread corrupt state"
      ]
    }
  ]
}
