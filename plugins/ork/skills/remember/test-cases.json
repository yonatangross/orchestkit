{
  "skill": "remember",
  "version": "1.0.0",
  "testCases": [
    {
      "id": "basic-orkremember-success-category-database",
      "rule": "",
      "query": "/ork:remember --success --category database We chose PostgreSQL with pgvector for RAG because it avoids a separate vector DB dependency",
      "expectedBehavior": [
        "Claude parses flags: outcome=success, category=database",
        "Extracts entities: PostgreSQL (Technology), pgvector (Technology), RAG (Technology)",
        "Detects relationship patterns: project USES pgvector, pgvector USED_FOR RAG",
        "Calls mcp__memory__create_entities with extracted entities and observations",
        "Calls mcp__memory__create_relations with detected relationships",
        "Searches for duplicates via mcp__memory__search_nodes before creating",
        "Confirms storage with SUCCESS label and graph entity/relation counts"
      ]
    },
    {
      "id": "edge-orkremember-failed-offset-pagination",
      "rule": "",
      "query": "/ork:remember --failed Offset pagination caused timeouts on tables with 1M+ rows. Should have used cursor-based pagination instead.",
      "expectedBehavior": [
        "Claude parses flags: outcome=failed",
        "Auto-detects category as pagination from keywords",
        "Creates entity with AntiPattern type for offset-pagination",
        "Extracts lesson from 'should have used' pattern: use cursor-based pagination",
        "Confirms storage with ANTI-PATTERN label and extracted lesson"
      ]
    },
    {
      "id": "negative-search-the-knowledge-graph",
      "rule": "",
      "query": "Search the knowledge graph for all decisions related to authentication",
      "expectedBehavior": [
        "Claude does NOT invoke the remember skill (remember is write-side only)",
        "Routes to the memory skill for search/read operations instead",
        "Calls mcp__memory__search_nodes via the memory skill, not remember"
      ]
    }
  ]
}
