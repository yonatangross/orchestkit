{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/schemas/mcp.schema.json",
  "mcpServers": {
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "description": "Up-to-date documentation for frameworks and libraries. PREREQUISITE: 22 of 36 ork agents grant mcp__context7__* and fall back to training data without it. Hosted HTTP is preferred over npx stdio, which spawns one child process per Claude Code session. Free tier (1000 requests, public repos) needs no key. For Context7 Pro, export CONTEXT7_API_KEY=ctx7sk-... and add: \"headers\": { \"Authorization\": \"Bearer ${CONTEXT7_API_KEY}\" }. Add the header ONLY once that variable is exported: an unexpanded literal connects, lists both tools, and then fails every query.",
      "auto": "75"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ],
      "description": "Complex reasoning and architectural decision support",
      "auto": "60"
    },
    "memory": {
      "command": "sh",
      "args": [
        "-c",
        "DIR=\"$CLAUDE_PROJECT_DIR\"; [ -n \"$DIR\" ] || DIR=\"$PWD\"; export MEMORY_FILE_PATH=\"$DIR/.claude/memory/memory.json\"; mkdir -p \"$DIR/.claude/memory\"; exec npx -y @modelcontextprotocol/server-memory"
      ],
      "description": "Knowledge graph memory (PRIMARY) - resolves an absolute MEMORY_FILE_PATH at runtime so the graph persists (#2631)",
      "auto": "90"
    },
    "tavily": {
      "command": "sh",
      "args": [
        "-c",
        "TAVILY_API_KEY=$(op read 'op://<vault>/Tavily API Key/API Key') exec npx -y tavily-mcp@latest"
      ],
      "description": "Web search, extract, crawl, and deep research",
      "auto": "70"
    }
  }
}
