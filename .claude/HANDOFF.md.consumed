# Session Handoff

**Branch**: main
**When**: 2026-03-24 18:13:30 UTC
**Session**: b355a2c8-4a3c-499c-99e4-3064a44b6d29

## Summary
Found it. Here's what happened:

The **latest commit on main** (`bee9be08`) is a squash-merged PR (#1136) from branch `feat/eval-pipeline-1045`. That PR included version bump commits to 7.22.0 **inside the squash** — you can see commits like `chore: bump to v7.22.0` and `docs: bump to v7.22.0` were part of that branch.

So the version numbers in `package.json`, `CLAUDE.md`, etc. were **manually bumped to 7.22.0** on that feature branch, but **release-please never created releases for 7.20, 7.21, or 7.22**. The last actual release-please release was v7.19.0 (`807ceab5`).

**The gap**: versions 7.20–7.22 were stamped into source files but never went through the release-please flow (no release PR merged, no GitHub release created, no git tag).

**To fix**, you have two options:

1.

## Modified Files
- .claude/HANDOFF.md.consumed

## Patterns Noted
- were **manually bumped to 7.22.0** on that feature branch, but **release-please never created releases for 7.20, 7.21, or 7.22**.
- **The gap**: versions 7.20–7.22 were stamped into source files but never went through the release-please flow (no release PR merged, no GitHub release created, no git tag).

## Recent Tasks
- Create mcp-visual-output skill (#1149) [completed]
- Create multi-surface-render skill (#1151) [completed]
- Upgrade ai-ui-generation — json-render first choice (#1153) [completed]
- Upgrade testing-integration + testing-e2e — emulate first choice (#1154) [completed]
- Upgrade design-to-code — Stage 4 RENDER (#1155) [completed]
- Create genui-architect + emulate-engineer agents (#1156, #1157) [completed]
- Upgrade playground — json-render output mode (#1159) [completed]
- Update manifests + build + test [completed]
