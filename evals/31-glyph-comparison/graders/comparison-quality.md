---
type: llm
focus: last_message
weight: 1
---
The user asked for a visual comparison of polling versus server-sent events, to
help them pick. Score the response against ALL of these claims:

1. The comparison is rendered as a drawn structure, a table or a side-by-side
   box, not as two paragraphs of prose.
2. The same dimensions are compared across both options, so the rows or columns
   line up. Listing three facts about A and two unrelated facts about B fails
   this claim.
3. The real trade-off is visible: B delivers faster with fewer requests, A is
   simpler and survives proxies that cut long-lived connections.
4. The response is compact and does not exceed roughly twelve lines of visual.
