#!/usr/bin/env bash
# Generated by OrchestKit Claude Plugin
# Created: 2026-09-21

# Rebuild the site's link card assets from committed inputs. No Higgsfield call:
# the art plate is already in art/. To regenerate the art itself (paid, capped),
# run gen.sh A first.
#   1. rasterize the eight host marks (host-marks.tsx + marks/)
#   2. write docs/site/assets/og/ (background, layout, fonts) for opengraph-image.tsx
#   3. write out/og-card.png, a full preview with today's counts
set -euo pipefail
cd "$(dirname "$0")"
node host_icons.mjs claude cursor codex muse agy devin opencode pi >/dev/null
python3 card.py --site
python3 card.py
