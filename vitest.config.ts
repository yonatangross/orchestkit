// Generated by OrchestKit Claude Plugin
// Created: 2026-03-03

// @ts-nocheck — vitest is installed in src/hooks/node_modules, not root
import { defineConfig } from './src/hooks/node_modules/vitest/dist/config.js';

/**
 * Root vitest config — scopes test discovery to src/hooks only.
 *
 * Without this, vitest 4.x auto-discovers **\/*.test.{ts,tsx} globally
 * and pulls in docs/site + orchestkit-demos tests that have uninstalled deps.
 *
 * Sub-projects run tests from their own directories:
 *   cd docs/site && npm test
 *   cd orchestkit-demos && npm test
 */
export default defineConfig({
  test: {
    projects: ['src/hooks'],
  },
});
