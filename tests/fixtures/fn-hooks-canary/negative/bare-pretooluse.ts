// Generated by OrchestKit Claude Plugin
// Created: 2026-09-14

// Fixture only, never loaded, and NOT listed in hooks.json. The canary script wraps it
// in a throwaway plugin and requires `claude plugin validate` to REJECT it (#3917).
//
// Bare `PreToolUse` was the one bare event name the validator accepted through CC
// 2.1.263. From 2.1.266 it is "not an event": the runtime dispatches shell PreToolUse
// hooks through the `classic.PreToolUse` site instead. Pinning the rejection means a
// revert upstream turns the probe red too, not only a further rename.
import type { Register } from "claude-code";

export const register: Register = (on) => {
  on("PreToolUse", {}, async ($, e, next) => next(e));
};
