# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: bare-eval
name: "Bare eval skill evaluation"
skill_path: src/skills/bare-eval/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Run isolated eval and grading calls using CC 2.1.81, bare mode."
    should_trigger: true

quality_evals:
  - prompt: "Run isolated eval and grading calls using CC 2.1.81, bare mode."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Run isolated eval and grading calls using CC 2.1.81, bare mode."

tags: [bare-eval, starter]
