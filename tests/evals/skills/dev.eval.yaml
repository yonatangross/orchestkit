# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: dev
name: "Dev skill evaluation"
skill_path: src/skills/dev/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "One-command dev loop boot."
    should_trigger: true

quality_evals:
  - prompt: "One-command dev loop boot."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: One-command dev loop boot."

tags: [dev, starter]
