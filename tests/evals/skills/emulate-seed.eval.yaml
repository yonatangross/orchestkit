# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: emulate-seed
name: "Emulate seed skill evaluation"
skill_path: src/skills/emulate-seed/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Generate emulate seed configs for stateful API emulation."
    should_trigger: true

quality_evals:
  - prompt: "Generate emulate seed configs for stateful API emulation."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Generate emulate seed configs for stateful API emulation."

tags: [emulate-seed, starter]
