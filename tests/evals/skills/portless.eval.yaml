# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: portless
name: "Portless skill evaluation"
skill_path: src/skills/portless/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Named HTTPS.localhost URLs for local development with portless (v0.15.x)."
    should_trigger: true

quality_evals:
  - prompt: "Named HTTPS.localhost URLs for local development with portless (v0.15.x)."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Named HTTPS.localhost URLs for local development with portless (v0.15.x)."

tags: [portless, starter]
