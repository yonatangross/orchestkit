# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: release-sync
name: "Release sync skill evaluation"
skill_path: src/skills/release-sync/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "push release content"
    should_trigger: true

quality_evals:
  - prompt: "push release content"
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Syncs latest release content to NotebookLM and HQ Knowledge Base after version tagging."

tags: [release-sync, starter]
