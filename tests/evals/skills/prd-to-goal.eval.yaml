# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: prd-to-goal
name: "Prd to goal skill evaluation"
skill_path: src/skills/prd-to-goal/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Decomposes a PRD, issue, or spec into a copy-pasteable `/goal until..."
    should_trigger: true

quality_evals:
  - prompt: "Decomposes a PRD, issue, or spec into a copy-pasteable `/goal until..."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Decomposes a PRD, issue, or spec into a copy-pasteable `/goal until..."

tags: [prd-to-goal, starter]
