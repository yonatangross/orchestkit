# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: expect
name: "Expect skill evaluation"
skill_path: src/skills/expect/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Diff-aware AI browser testing, analyzes git changes, generates targeted test plans, and executes them via agent-browser (Rust daemon + CDP, ARIA-tree-first)."
    should_trigger: true

quality_evals:
  - prompt: "Diff-aware AI browser testing, analyzes git changes, generates targeted test plans, and executes them via agent-browser (Rust daemon + CDP, ARIA-tree-first)."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Diff-aware AI browser testing, analyzes git changes, generates targeted test plans, and executes them via agent-browser (Rust daemon + CDP, ARIA-tree-first)."

tags: [expect, starter]
