# Generated by OrchestKit Claude Plugin
# Updated: 2026-03-21 — Fixed assertions to match actual skill content

id: memory-fabric
name: "Memory fabric evaluation"
skill_path: src/skills/memory-fabric/SKILL.md
plugin_dir: plugins/ork

quality_evals:
  - prompt: |
      I want to search my knowledge graph for what the database-engineer
      agent recommended about pagination. How does the memory fabric
      find and rank relevant results?
    assertions:
      - name: "graph query tool"
        check: "recommends using mcp__memory__search_nodes to search the knowledge graph"
      - name: "entity extraction"
        check: "extracts entity hints like 'database-engineer' and 'pagination' from the query"
      - name: "query parsing"
        check: "describes a query parsing step that breaks the user question into searchable terms"

  - prompt: |
      My memory search returns duplicate results that say the same
      thing in slightly different words. How does the fabric handle
      deduplication?
    assertions:
      - name: "dedup strategy"
        check: "describes a deduplication approach for similar memory results"
      - name: "merge or filter"
        check: "explains whether duplicates are merged into one result or filtered out"

  - prompt: |
      How does the memory fabric connect related concepts across
      different graph entities? For example, linking a technology
      recommendation to the agent who made it.
    assertions:
      - name: "entity relationships"
        check: "describes how entities in the graph are connected via relations"
      - name: "cross-referencing"
        check: "explains how searching one entity can surface related entities through graph connections"

tags: [memory-fabric, background, ai-ml]
