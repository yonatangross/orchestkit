# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: ci-debug
name: "Ci debug skill evaluation"
skill_path: src/skills/ci-debug/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Diagnose a failing CI run against an 11-pattern playbook."
    should_trigger: true

quality_evals:
  - prompt: "Diagnose a failing CI run against an 11-pattern playbook."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Diagnose a failing CI run against an 11-pattern playbook."

tags: [ci-debug, starter]
