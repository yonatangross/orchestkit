# Generated by OrchestKit Claude Plugin
# Created: 2026-04-01

id: dream
name: "Dream skill evaluation"
skill_path: src/skills/dream/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  # Should trigger — direct requests
  - prompt: "consolidate my memory files"
    should_trigger: true
  - prompt: "clean up stale memory entries"
    should_trigger: true
  - prompt: "run dream to prune old memories"
    should_trigger: true
  # Should trigger — casual/indirect phrasing
  - prompt: "my memory files are a mess, can you tidy them up"
    should_trigger: true
  - prompt: "prune memories that reference files that no longer exist"
    should_trigger: true
  # Should NOT trigger — remember confusion pair (store vs consolidate)
  - prompt: "remember that we chose Postgres over MongoDB"
    should_trigger: false
  - prompt: "save this decision about caching"
    should_trigger: false
  # Should NOT trigger — memory search (read-side, not maintenance)
  - prompt: "search memory for past database decisions"
    should_trigger: false
  - prompt: "what did we decide about pagination last time"
    should_trigger: false
  # Should NOT trigger — adjacent tasks
  - prompt: "implement the user authentication flow"
    should_trigger: false

quality_evals:
  - prompt: |
      I have about 30 memory files that have accumulated over the last
      few months. Some reference files that were deleted during a refactor.
      How would you consolidate them?
    assertions:
      - name: "staleness detection"
        check: "describes checking whether referenced file paths still exist using Glob or filesystem checks"
      - name: "deterministic approach"
        check: "emphasizes deterministic verification (file exists, symbol exists) rather than LLM judgment for pruning decisions"
      - name: "dry run option"
        check: "mentions or offers a --dry-run flag to preview changes before applying"
      - name: "index rebuild"
        check: "mentions rebuilding MEMORY.md index after pruning"

tags: [dream, user-invocable, memory, maintenance]
