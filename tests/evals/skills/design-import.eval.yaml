# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: design-import
name: "Design import skill evaluation"
skill_path: src/skills/design-import/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Imports a Claude Design (claude.ai/design) handoff bundle and scaffolds the proposed components into the project."
    should_trigger: true

quality_evals:
  - prompt: "Imports a Claude Design (claude.ai/design) handoff bundle and scaffolds the proposed components into the project."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Imports a Claude Design (claude.ai/design) handoff bundle and scaffolds the proposed components into the project."

tags: [design-import, starter]
