# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: telemetry-inspect
name: "Telemetry inspect skill evaluation"
skill_path: src/skills/telemetry-inspect/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Inspects the OrchestKit telemetry pipeline for the current project, lists all known telemetry files with write counts, sizes, schema status, growth trend, and orphan detection."
    should_trigger: true

quality_evals:
  - prompt: "Inspects the OrchestKit telemetry pipeline for the current project, lists all known telemetry files with write counts, sizes, schema status, growth trend, and orphan detection."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Inspects the OrchestKit telemetry pipeline for the current project, lists all known telemetry files with write counts, sizes, schema status, growth trend, and orphan detection."

tags: [telemetry-inspect, starter]
