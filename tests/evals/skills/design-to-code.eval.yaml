# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: design-to-code
name: "Design to code skill evaluation"
skill_path: src/skills/design-to-code/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "screenshot to component"
    should_trigger: true

quality_evals:
  - prompt: "screenshot to component"
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Mockup-to-component pipeline using Google Stitch, 21st.dev, and Storybook MCP."

tags: [design-to-code, starter]
