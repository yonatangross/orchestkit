# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: swarm-migrate
name: "Swarm migrate skill evaluation"
skill_path: src/skills/swarm-migrate/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Cross-repo migration swarm, one coordinator + N parallel subagents (one per target repo) that apply the same transformation, open PRs, wait for CI, and report back to a shared JSON ledger."
    should_trigger: true

quality_evals:
  - prompt: "Cross-repo migration swarm, one coordinator + N parallel subagents (one per target repo) that apply the same transformation, open PRs, wait for CI, and report back to a shared JSON ledger."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Cross-repo migration swarm, one coordinator + N parallel subagents (one per target repo) that apply the same transformation, open PRs, wait for CI, and report back to a shared JSON ledger."

tags: [swarm-migrate, starter]
