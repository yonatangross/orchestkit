# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: ci-sentinel
name: "Ci sentinel skill evaluation"
skill_path: src/skills/ci-sentinel/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "Hourly autonomous classifier for failing PRs across your repos."
    should_trigger: true

quality_evals:
  - prompt: "Hourly autonomous classifier for failing PRs across your repos."
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Hourly autonomous classifier for failing PRs across your repos."

tags: [ci-sentinel, starter]
