# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: design-stylecards
name: "Design stylecards skill evaluation"
skill_path: src/skills/design-stylecards/SKILL.md
plugin_dir: plugins/ork

trigger_evals:
  - prompt: "make it look polished"
    should_trigger: true

quality_evals:
  - prompt: "make it look polished"
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Declarative catalog of named aesthetic recipes, exact shadow stacks, glass surfaces, gradient treatments, and type scales as copy-paste values with Use-When and Avoid rules."

tags: [design-stylecards, starter]
