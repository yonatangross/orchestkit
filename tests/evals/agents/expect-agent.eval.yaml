# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: expect-agent
name: "Expect agent agent evaluation"
agent_path: src/agents/expect-agent.md

agent_evals:
  - prompt: "Browser test execution: runs diff-aware test plans via agent-browser with ARIA selectors, status protocol, and 6-category failure classification."
    scaffold: empty
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Browser test execution: runs diff-aware test plans via agent-browser with ARIA selectors, status protocol, and 6-category failure classification."

tags: [expect-agent, agent, starter]
