# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: design-context-extractor
name: "Design context extractor agent evaluation"
agent_path: src/agents/design-context-extractor.md

agent_evals:
  - prompt: "Design context extraction: analyzes screenshots, URLs, or live apps to extract color palettes, typography, spacing, and component patterns as structured design tokens."
    scaffold: empty
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Design context extraction: analyzes screenshots, URLs, or live apps to extract color palettes, typography, spacing, and component patterns as structured design tokens."

tags: [design-context-extractor, agent, starter]
