# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: component-curator
name: "Component curator agent evaluation"
agent_path: src/agents/component-curator.md

agent_evals:
  - prompt: "Component library curator: audits project component usage, searches 21st.dev registry for alternatives, tracks component freshness, and recommends upgrades for design consistency."
    scaffold: empty
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Component library curator: audits project component usage, searches 21st.dev registry for alternatives, tracks component freshness, and recommends upgrades for design consistency."

tags: [component-curator, agent, starter]
