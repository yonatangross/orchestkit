# Generated by OrchestKit eval-coverage --fill (#2192)
# STARTER spec: trigger prompt + assertion are derived mechanically from
# the component description. Refine both before trusting the scores.
# Created: 2026-07-18

id: claude-design-orchestrator
name: "Claude design orchestrator agent evaluation"
agent_path: src/agents/claude-design-orchestrator.md

agent_evals:
  - prompt: "Parses claude.ai/design handoff bundles: validates schema, dedups proposed components against the codebase via component-search, reconciles tokens, and tracks bundle→PR provenance so design intent..."
    scaffold: empty
    assertions:
      - name: "produces relevant output"
        check: "produces output consistent with: Parses claude.ai/design handoff bundles: validates schema, dedups proposed components against the codebase via component-search, reconciles tokens, and tracks bundle→PR provenance so design intent..."

tags: [claude-design-orchestrator, agent, starter]
