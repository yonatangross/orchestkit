#!/bin/bash
# Generated by OrchestKit Claude Plugin
# Created: 2026-07-14

# Hook Registry Closure Gate — thin wrapper around the real validator.
#
# Asserts every hook registered in src/hooks/src/entries/*.ts is reachable from
# hooks.json ∪ agent/skill frontmatter ∪ dispatcher fan-out, that no dispatch
# path references an unregistered id (ghost), and that the count accounting in
# hooks.json's description matches reality. Ratchet baseline:
# src/hooks/scripts/known-dead-hooks.json (only shrinks — see #959 / #2886).
#
# Exit codes: 0 = closure holds, 1 = drift found
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "${SCRIPT_DIR}/../../src/hooks/scripts/validate-registry.mjs"
