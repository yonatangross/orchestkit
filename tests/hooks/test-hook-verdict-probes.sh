#!/usr/bin/env bash
# Generated by OrchestKit Claude Plugin
# Created: 2026-08-29
# tests/hooks/test-hook-verdict-probes.sh
#
# Verdict probes (#3801 follow-up): every decisive hook is driven through the
# real runner at its hooks.json entry with a fixture built to trip it, and the
# VERDICT is asserted (deny / ask / block / context), with a control fixture
# that must stay silent. "ok:true" in hook-timing only ever meant "did not
# throw"; this is the gate that says "did its job". See verdict-probes/.
set -euo pipefail
PROJECT_ROOT="${CLAUDE_PROJECT_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)}"
cd "$PROJECT_ROOT"

echo "Hook Verdict Probes"
echo "==================="
if [ ! -f plugins/ork/hooks/bin/run-hook.mjs ]; then
  echo "FAIL: plugins/ork is not built; run npm run build"
  exit 1
fi
node tests/hooks/verdict-probes/run-probes.mjs tests/hooks/verdict-probes/probes.json
