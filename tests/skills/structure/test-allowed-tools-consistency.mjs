#!/usr/bin/env node
// Generated by OrchestKit Claude Plugin
// Created: 2026-09-10
/**
 * CI arm for scripts/allowed-tools-check.mjs (#4008).
 *
 * The checker itself lives under scripts/ so a human can run it with --report.
 * This thin wrapper is what puts it in CI: scripts/ci/run-tests.sh discovers
 * tests/skills by DIRECTORY (ci.yml:808), so a file here is picked up with no
 * roster edit, the same way tests/plugins gets its arms.
 *
 * Exits with the checker's own status, so a NEW contradiction or a STALE
 * baseline entry turns this red.
 */
import { spawnSync } from 'node:child_process';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const REPO_ROOT = join(dirname(fileURLToPath(import.meta.url)), '..', '..', '..');
const result = spawnSync(
  process.execPath,
  [join(REPO_ROOT, 'scripts', 'allowed-tools-check.mjs')],
  { stdio: 'inherit', cwd: REPO_ROOT },
);
process.exit(result.status ?? 1);
