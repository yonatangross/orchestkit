// Generated by OrchestKit Claude Plugin
// Created: 2026-04-25

import { defineConfig } from 'vitest/config';
import { resolve } from 'node:path';

/**
 * Property-test runner config (#1452).
 *
 * Runs separately from the regular vitest suite — fast-check generates many
 * inputs per assertion and adds noticeable runtime that we don't want on
 * every PR check. CI invokes this via `npm run test:property` in its own job.
 */
export default defineConfig({
  test: {
    globals: false,
    environment: 'node',
    setupFiles: ['./vitest.setup.ts'],
    include: ['src/__tests__/property/**/*.property.test.ts'],
    exclude: ['**/node_modules/**', '**/dist/**'],
    alias: {
      '@bin': resolve(__dirname, 'bin'),
    },
    // Property tests can run longer than the 5s default — grant headroom.
    testTimeout: 30000,
  },
});
