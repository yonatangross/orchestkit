// Generated by OrchestKit Claude Plugin
// Created: 2026-08-08

// Type declarations for the generated CC output-key contract.
//
// The .mjs is generated from spec/cc-output-keys.spec.yml; this file exists so
// tsconfig.test.json can typecheck importers of it. CI's Static Analysis job
// runs `tsc -p tsconfig.test.json`, which is a DIFFERENT config from the
// `npm run typecheck` used locally — the local run passed while CI failed with
// TS7016 on this exact import.

/** CC version the contract was derived from. */
export declare const CC_VERSION: string;

/** Events on which CC reads hookSpecificOutput at all. */
export declare const EVENTS_WITH_HOOK_EVENT_NAME: Set<string>;

/** Events on which CC reads additionalContext. */
export declare const EVENTS_WITH_ADDITIONAL_CONTEXT: Set<string>;

/** Per-key event allow-lists beyond hookEventName / additionalContext. */
export declare const KEY_EVENTS: Map<string, Set<string>>;

/** Keys the spec records as UNVERIFIED. The guard must never strip these. */
export declare const UNVERIFIED_KEYS: Set<string>;
