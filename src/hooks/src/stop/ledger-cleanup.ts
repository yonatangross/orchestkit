// Generated by OrchestKit Claude Plugin
// Created: 2026-04-05

/**
 * Ledger Cleanup — removes stale agent attribution ledgers on session stop.
 *
 * Extracted from stop/unified-dispatcher.ts inline lambda for
 * v7.30.0 dispatcher flattening (#1264).
 */

import type { HookInput, HookResult, HookContext } from '../types.js';
import { outputSilentSuccess } from '../lib/common.js';
import { cleanupStaleLedgers } from '../lib/agent-attribution.js';
import { NOOP_CTX } from '../lib/context.js';

export function ledgerCleanup(_input: HookInput, _ctx: HookContext = NOOP_CTX): HookResult {
  try {
    cleanupStaleLedgers();
  } catch {
    // Silent — cleanup is best-effort
  }
  return outputSilentSuccess();
}
