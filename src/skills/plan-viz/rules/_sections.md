---
title: Plan Viz Rule Categories
version: 1.0.0
---

# Rule Categories

No local rules — ASCII diagram patterns moved to the `ascii-visualizer` skill.
Plan-viz depends on `ascii-visualizer` via the `skills:` frontmatter field.
